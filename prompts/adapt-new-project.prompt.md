---
mode: agent
description: Adapt the scaffolded scripts to a brand-new ADO project / plan.
---

I am cloning this folder for a new ADO project. Walk me through every
constant I have to change in `daily_report.py` and `run_daily.ps1`, and call
out anything project-specific (custom bug fields, iteration path shape,
whether the iteration is the project root or a leaf). Then give me a
one-shot manual test command that runs the Python end-to-end with my token.
